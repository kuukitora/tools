// maimai rival inject script

(function() {
    const rivalData = {}; // Object to hold rival data

    function injectRivalData(rivalId, rivalScore) {
        // Logic to inject rival data into the game
        console.log(`Injecting rival data: ${rivalId} with score: ${rivalScore}`);
        // Example: rivalData[rivalId] = rivalScore;
    }

    // Example usage
    injectRivalData('rival_1', 98000);
    injectRivalData('rival_2', 92000);

})();